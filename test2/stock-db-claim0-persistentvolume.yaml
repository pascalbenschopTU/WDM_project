apiVersion: v1
kind: PersistentVolume
metadata:
  name: stock-db-claim0
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "postgres-data"